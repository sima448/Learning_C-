#include<bits/stdc++.h>
using namespace std;

void reverse(vector<int>& v, int i , int n){
   // if(i == n - 1) return v[i];
   //return max;
}
int main(){
    int n;
    cin>> n;
    vector<int> v(n);
    for(int& i : v) cin >> i;
    
}