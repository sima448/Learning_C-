//you are given an array of N elements and you have to tell the numbers of pairs (i,j) such that 1<=i<j<=N and(i-j = arr[i] - arr[j]).
#include<bits/stdc++.h>
using namespace std;
int main() {
    int n;
    cin>>n;
    vector<int>v(n);
    for(int i = 0; i < n; i++){
        cin>>v[i];
    }
    
   
}